"""Voice Activity Detection (VAD) with a 1D ResNet in PyTorch.

Modules:
    model: The ``Resnet1D`` architecture and its ``ModelConfig``.
    data: Feature extraction, dataset building, and the PyTorch DataLoader.
    train: Training loop and model export.
    inference: Sliding-window inference and visualization.
"""
